#
# Triggers
#

# No CI trigger
trigger: none

# Daily build. Time is UTC.
schedules:
- cron: "0 10 * * *"
  displayName: Daily build
  branches:
    include:
    - main
  always: false

#
# Extend build stages
#
extends:
  template: build/azure-pipeline/build-stages.yml